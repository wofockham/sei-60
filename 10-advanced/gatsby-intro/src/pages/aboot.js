import * as React from 'react';
import Layout from '../components/layout';

const AbootPage = () => {
    return (
        <Layout pageTitle="About Us">
            <p>Content about us coming soon</p>
        </Layout>
    );
}

export const Head = () => <title>About Us</title>;

export default AbootPage;
